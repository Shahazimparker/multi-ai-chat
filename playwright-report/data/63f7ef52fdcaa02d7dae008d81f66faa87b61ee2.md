# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.real.spec.ts >> configured admin user can reach admin page
- Location: e2e-real\auth.real.spec.ts:27:5

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: locator('.admin-brand')
Expected substring: "Admin"
Timeout: 20000ms
Error: element(s) not found

Call log:
  - Expect "toContainText" with timeout 20000ms
  - waiting for locator('.admin-brand')
    - waiting for" http://localhost:3000/login" navigation to finish...
    - navigated to "http://localhost:3000/login"

```

```yaml
- button "Toggle theme":
  - img
- img
- text: Miles Intelligence
- paragraph: Unified AI models in one place
- text: Gemini Groq Mistral Claude
- img
- textbox "Username or Email"
- img
- textbox "Password"
- button:
  - img
- checkbox "Remember me"
- text: Remember me
- button "Sign In"
```

# Test source

```ts
  1  | import { expect, test } from '@playwright/test';
  2  | import { REAL_ADMIN, REAL_ADMIN_PASSWORD, REAL_USER, REAL_PASSWORD, login, requireRealUser } from './support/realHelpers';
  3  | 
  4  | test.beforeEach(() => {
  5  |   requireRealUser();
  6  | });
  7  | 
  8  | test('redirects unauthenticated users to login', async ({ page }) => {
  9  |   await page.goto('/chat');
  10 |   await expect(page).toHaveURL(/\/login$/);
  11 | });
  12 | 
  13 | test('logs in as configured real user and can log out', async ({ page }) => {
  14 |   await login(page, REAL_USER, REAL_PASSWORD);
  15 |   await expect(page).toHaveURL(/\/(chat|admin)$/);
  16 | 
  17 |   if (page.url().includes('/chat')) {
  18 |     await expect(page.locator('.sidebar-footer')).toContainText(REAL_USER);
  19 |     await page.locator('.sidebar-footer .logout-btn').click();
  20 |   } else {
  21 |     await page.getByRole('button', { name: /Logout/i }).last().click();
  22 |   }
  23 | 
  24 |   await expect(page).toHaveURL(/\/login$/);
  25 | });
  26 | 
  27 | test('configured admin user can reach admin page', async ({ page }) => {
  28 |   test.skip(!REAL_ADMIN || !REAL_ADMIN_PASSWORD, 'No real admin credentials configured');
  29 |   await login(page, REAL_ADMIN, REAL_ADMIN_PASSWORD);
  30 |   await page.goto('/admin');
  31 |   test.skip(!/\/admin$/.test(page.url()), 'Configured admin credentials do not have admin access in this environment');
> 32 |   await expect(page.locator('.admin-brand')).toContainText('Admin');
     |                                              ^ Error: expect(locator).toContainText(expected) failed
  33 | });
  34 | 
```